<html>

    <head>
        {{#pageTitle}}
            <title>{{pageTitle}}</title>
        {{/pageTitle}}
        {{^pageTitle}}
            <title>{{h.defaultPageTitle}}</title>
        {{/pageTitle}}
    </head>
    <body>
        <h1>Cabeçalho {{teste}}</h1>
        <hr/>
        {{#h.menu}}
            <li><a href="{{slug}}">{{name}}</a></li>
        {{/h.menu}}
    </body>
</html>
